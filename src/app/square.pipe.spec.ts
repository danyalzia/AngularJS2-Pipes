/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SquarePipe } from './square.pipe';

describe('Pipe: Square', () => {
  it('create an instance', () => {
    let pipe = new SquarePipe();
    expect(pipe).toBeTruthy();
  });
});
